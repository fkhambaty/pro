# Okavo welcomes crawlers, indexers and answer engines. Only the signed-in
# workspace is off limits, because it holds private contracts.
#
# Deliberately one wildcard group. Under RFC 9309 an agent with no group of
# its own falls back to "*", so this covers Googlebot, Bingbot,
# Google-Extended, GPTBot, ClaudeBot, PerplexityBot and everything else,
# present and future. Naming agents individually only creates ways to get the
# inheritance wrong.

User-agent: *
Allow: /
Disallow: /app
Disallow: /api

Sitemap: https://okavo.org/sitemap.xml
